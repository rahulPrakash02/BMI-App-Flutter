import 'package:flutter/material.dart';

const Color appBarColor = Color(0xFF1A374D);
const Color secondaryColor = Color(0xFF406882);
const Color primaryColor = Color(0xFF6998AB);
const Color textColor = Color(0xFFB1D0E0);
const Color underColor = Colors.lightBlueAccent;
const Color normalColor = Colors.lightGreenAccent;
const Color overColor = Colors.yellow;
const Color obeseColor = Colors.orangeAccent;

//styles
const TextStyle textStyling = TextStyle(color: textColor, fontSize: 16.0);

const TextStyle underStyling =
    TextStyle(color: underColor, fontSize: 20.0, fontWeight: FontWeight.bold);

const TextStyle normalStyling =
    TextStyle(color: normalColor, fontSize: 20.0, fontWeight: FontWeight.bold);

const TextStyle overStyling =
    TextStyle(color: overColor, fontSize: 20.0, fontWeight: FontWeight.bold);

const TextStyle obeseStyling =
    TextStyle(color: obeseColor, fontSize: 20.0, fontWeight: FontWeight.bold);
